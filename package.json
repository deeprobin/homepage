{
  "name": "root",
  "version": "0.0.0",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/deeprobin/homepage.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "^14.18.0 || >=16.12.0",
    "pnpm": ">=7.9.5"
  },
  "packageManager": "pnpm@7.12.2",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^1.6.2"
  }
}
